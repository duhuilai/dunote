# v0.1.42

## 优化：中文日期选择器支持手动录入与年/月快速切换
- 之前的 `DateField` 只能逐月点选，不能手动输入，也无法跳年/跳月。
- 触发区改为**可编辑输入框**，可直接录入 `YYYY-MM-DD`（回车 / 失焦自动解析，非法输入则还原）。
- 点开日历后：
  - 标题显示「年 月」，点击年份区域进入**月份选择**视图（1–12 月网格），
  - 再点击顶部年份标题进入**年份选择**视图（前后各 6 年的网格），选中后逐级回到日历。
- 仍保留「今天」「清除」快捷操作。
- 人员在「新增/编辑」弹窗中的入职日期已具备以上能力。
