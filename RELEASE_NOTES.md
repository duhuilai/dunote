# v0.2.25

## 优化：本地保存版本的生成时机
上一版（v0.2.24）中快照是在「发起落盘时」无条件留档，可能出现版本与磁盘实际内容不一致的情况。本版按「**实文件自动保存成功后才生成**」调整：

- 留档逻辑改挂在 `writeTextFile` 的**成功回调**上——只有文件真正写入成功才生成快照。写盘失败、或内容为空被拦截（避免清空文件）时均不留档，杜绝产生与实际内容不符的假版本。
- 连续输入过程中的常规自动保存按 **15 秒节流**，避免高频刷档；**切换笔记 / 关闭前的落盘强制留档**（绕过节流），保证关键节点一定有档可循。
- 说明：仅**本地文件笔记**会产生保存版本；普通（内存）笔记不写磁盘，因此没有文件保存版本。历史面板空态文案已同步为「本地文件笔记在每次自动保存成功后会自动留档」。

## 承接 v0.2.24 的其余改动
- 本地保存版本快照（`src/utils/noteSnapshots.ts`）：`appConfigDir/snapshots/<笔记>/<时间戳>.html` + 轻量 `<时间戳>.meta.json`，每篇保留最近 30 份，列表只读元数据不载入含 base64 图片的大正文。
- 历史面板新增「历史记录 / 本地保存版本」页签，支持预览 / 恢复此版本 / 删除。
- 修复本地文件读取失败时把笔记清空 / 串写成上一篇内容的问题（读取失败即放弃加载，保持原内容并提示）。
- 修复表格无法插入行/列的 v0.2.22 回归（`ImeGuard` 宽限期卡死 + 结构性编辑被误拦）。
