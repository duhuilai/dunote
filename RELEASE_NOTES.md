# v0.2.15

## 新增
- 笔记列表手动排序：支持文件夹之间、文件夹内笔记调整顺序。拖拽或右键「上移 / 下移」即可调整；顺序自动落盘（本地根目录 `.dunote-order.json`），重开软件保持。
- 任务管理增强：新增「按负责人 / 参与人」筛选；任务支持删除（带二次确认）；任务日期选择器统一为中文三级（年 / 月 / 日）选择器，与人员管理一致。
- 人员管理新增「离职日期」字段，离职人员状态可标记。
- 逾期任务全局常驻提示：超期未完成任务在任意页面顶部常驻提醒（OverdueNotifier）。
- macOS 文件夹访问授权缓解：引入 `tauri-plugin-persisted-scope` 持久化授权范围；恢复上次文件夹时若访问失败，侧栏显示「重新授权文件夹」一键引导。构建脚本支持可选的 Apple 证书签名（在 GitHub Secrets 配置后自动生效）。

## 修复
- 修复 Windows 下笔记列表 HTML5 拖拽排序失效：关闭 Tauri 原生窗口拖放拦截（`dragDropEnabled: false`），使 webview 内拖拽事件正常接收。
