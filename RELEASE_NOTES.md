# v0.2.28

## 调整：自动保存触发条件

自动保存改为**仅以下两种情况触发，且均要求「文档确实发生变化」**（与上次加载/保存的内容不一致才落盘，无变化不写盘）：

1. **停止操作满 5 秒**：把空闲判定从 1.5 秒调整为 **5 秒**（常量 `AUTOSAVE_IDLE_MS = 5000`）。每次编辑都会重置计时器，因此是「5 秒内没有任何操作」才保存，而不是固定每 5 秒写一次——连续输入过程中不会产生中间态的频繁写盘。
2. **切换文档时**：由切换流程中的 `flushPending()` 立即落盘，同样只在内容有变化时才写。

## 说明

- 切换文档这一步此前已按「内容变化才写」实现，本次未改动逻辑，仅统一了注释与语义。
- 由于每次自动保存成功后都会生成一个本地实时保存版本（v0.2.26 起），把空闲时长放宽到 5 秒也会相应减少版本留档的频率与磁盘占用——连续输入时只会在停下 5 秒后留一版，而不是每 1.5 秒一版。
