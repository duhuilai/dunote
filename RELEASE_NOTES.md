## duNote v0.1.23

### 新增 / 改进
- 安装更新前自动关闭当前应用窗口，避免 Windows 安装程序提示需要先关闭旧实例。
- 编辑器代码块背景改为浅色系（#F1F5F9），文字颜色改为深色，降低视觉反差。

### 修复
- 修复快速双击「生成历史」按钮仍会生成两条历史记录的问题：改用 useRef 同步锁，在 React 批量更新期间也能阻止重复触发。
- 移除设置页「自定义类型字体颜色」区块，避免冗余配置。
