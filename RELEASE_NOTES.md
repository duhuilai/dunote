# v0.2.27

## 修复
- **打开历史面板 / 点击「实时历史恢复」时整页白屏（React error #310）**：v0.2.26 新增的三个快照回调（预览 / 恢复 / 删除）误放在了 `if (!showHistory) return null` **之后**，而它们是 `useCallback` Hook——弹窗从「关闭」变「打开」时组件渲染的 Hook 数量增加，触发 React 的 Hook 顺序校验崩溃，被 ErrorBoundary 捕获后显示「出错了」。已把这三个回调移到 early return 之前（与文件内既有的同类注释要求一致），并已全文件排查确认其余 Hook 均在 early return 之前。

## 承接 v0.2.26（本地实时保存版本）
- 每次自动保存（文件写入成功）后自动生成版本，内容指纹去重（与最新一版相同不重复留档）。
- 启动时清理过期版本：最近 **5 版**无条件保留、超过 **7 天**删除、硬上限 **200 条**。
- 编辑器工具栏新增蓝色「**实时历史恢复**」按钮，直接打开「本地保存版本」页签，可预览 / 恢复 / 删除任意一次自动保存的版本。
