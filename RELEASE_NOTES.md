# duNote v0.1.26

## 修复 · 点击安装后自动退出程序
- macOS 端点击「安装」后，原逻辑只关闭了窗口、App 进程仍驻留在 Dock 中运行，导致安装包（dmg）无法替换正在运行的旧 app，必须手动退出。
- 改为：先打开安装包（挂载 dmg / 启动安装向导），等待系统启动安装程序后再调用进程退出，确保整个 duNote 进程真正结束，安装可顺利进行（Windows 端同样受益，避免被提示先关闭旧实例）。
