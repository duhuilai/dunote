# v0.2.26

## 本地实时保存版本

- **每次自动保存都生成版本**：去掉了上一版的 15 秒节流，`本地文件笔记`在**每次自动保存（文件写入成功）后**都会留一个版本。同时加入内容指纹去重——与最新一版内容完全一致时不再重复留档，避免空转刷出大量重复版本。
- **启动时清理过期版本**：应用启动时统一执行 `pruneAllSnapshotsOnStartup()`，遍历所有笔记的版本目录执行保留策略：
  1. **最近 5 版无条件保留**（即使已全部过期，保证清理后仍有可找回的版本）；
  2. 超过 **7 天**的版本删除；
  3. 硬上限 **200 条**，超出删除最旧的。
- **新增「实时历史恢复」按钮**：编辑器工具栏新增独立按钮（蓝色，位于「恢复历史」左侧），点击后直接打开历史弹窗并**默认落在「本地保存版本」页签**，可从任意一次自动保存的版本预览 / 恢复 / 删除。原有的「恢复历史」按钮仍打开 git 历史页签，两者互不干扰。

## 说明与边界
- 版本仅在**本地文件笔记**真实写入成功后生成；普通（内存）笔记不写磁盘，因此没有文件保存版本。
- 版本正文存于 `appConfigDir/snapshots/<笔记>/<时间戳>.html`，元数据（含内容指纹）单独存 `.meta.json`——列表只读轻量元数据，不会把含 base64 图片的大正文整体载入内存。
- 清理逻辑保证「至少保留最近 5 版」，因此即使长时间未启动应用，也不会出现版本被清空的情况。
