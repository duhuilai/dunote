# duNote v0.1.28

## 优化 · 智能表格 UI 与交互
- 行删除按钮列固定为 36px，hover 行时显示删除图标，平时只显示行号
- 表头支持**拖拽调整列宽**，列宽持久化到文档
- 表格区域设置 `max-height` 并固定表头（`position: sticky`），行多时可滚动查看
- 列标题仅显示列名和「⋯」设置按钮，不再显示类型/选项标签
- 列类型、选项管理、删除列等操作全部集中到「⋯」列设置菜单内
