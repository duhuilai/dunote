# v0.2.14

> 修复智能表格中文输入法录入时光标跳前/多出换行；插入行列支持选择方向与批量数量。

## 修复
- 现象：智能表格文本单元格用输入法录入中文时，光标跳到文字最前面、自动保存多出一行空白（英文/复制正常）。
- 根因：受控 textarea/input 在 IME 合成期间被 React 重设 value 打断合成。改为非受控（uncontrolled）：`defaultValue` 初始化、IME 全程由浏览器原生管理，`onBlur`/`onCompositionEnd` 时才读取 DOM 值提交，外部更新仅在不聚焦时同步。

## 新增
- 智能表格工具栏的「+ 行」「+ 列」合并为「+ 插入」下拉，含 4 个方向：上方插行 / 下方插行 / 左侧插列 / 右侧插列。
- 下拉顶部新增「插入数量」输入框（默认 1，范围 1–100，支持手动输入或 −/+ 步进），可一次插入多行/多列。
